document.addEventListener('DOMContentLoaded',()=>{const grid=document.querySelector('#featuredProducts');if(!grid)return;PRODUCTS.slice(0,6).forEach(p=>{const el=document.createElement('article');el.className='product-card';el.innerHTML=`<div class="product-image"><img src="${p.image}" alt="${p.name}"></div><div class="product-body"><span class="tag">${p.category.replace('-',' ')}</span><h3>${p.name}</h3><p class="product-price">${money(p.price)} <small>/ ${p.measure}</small></p><button class="cart btn" >Add to Cart</button></div>`;el.querySelector('.cart').onclick=()=>addToCart(p);grid.appendChild(el)})});
